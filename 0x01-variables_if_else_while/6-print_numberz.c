#include <stdio.h>

/**
 * main - All single digit number in base 10
 *
 * Decription: Starting from 0
 * Return: Always(0) Success
 */

int main(void)
{
	int n = 48;

	while (n < 58)
	{
		putchar(n);
		n++;
	}
	putchar ('\n');

	return (0);
}
